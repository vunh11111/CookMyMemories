server:
  port: ${PORT:8080}  # Render sẽ cung cấp PORT environment variable

spring:
  datasource:
    url: ${DATABASE_URL}  # Render database URL
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        
  # Profile cho production
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:production}

---
# Development profile
spring:
  config:
    activate:
      on-profile: development
  datasource:
    url: jdbc:mysql://localhost:3306/mydb
    username: root
    password: password
  jpa:
    show-sql: true

---
# Production profile  
spring:
  config:
    activate:
      on-profile: production